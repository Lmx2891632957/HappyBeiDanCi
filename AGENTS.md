# AGENTS.md

> 本文件对在本仓库中工作的所有 AI 智能体与协作者生效。它是仓库的"行为守则"：动手前先读，改动前先对齐，提交前先检查。
> 当前仓库处于**骨架搭建阶段**：Flutter 工程骨架与基础设施（分层、数据库、i18n、接口定义）已就绪，学习/复习/FSRS 等业务逻辑尚未实现；本文档描述的目录结构为实现基准，实际差异见 TECH_DOC.md §4 结构补充说明。

---

## 1. 项目概述

- 产品：背单词软件（Android 首版，Flutter 实现），面向高中生，完全免费。
- 应用名：**我爱背单词**（包名 `com.woaibeidanci.app`）；远程仓库托管于 GitHub（`origin`）。
- 权威文档：
  - [PRD.md](./PRD.md) —— 产品需求，已定稿，任何实现不得违背其中"已确认决策"。
  - [TECH_DOC.md](./TECH_DOC.md) —— 技术设计，含架构、算法、数据模型、内容管线。
- 核心技术栈：Flutter 3.x / Dart 3、Riverpod、Drift（SQLite + WAL）、FSRS-5 复习算法、just_audio、WorkManager。
- 核心约束：本地优先、离线可用、无账号体系、无广告/统计/支付 SDK、未成年人最小化采集。
- 需求与技术选型已确认（见 PRD.md §11 决策记录与 TECH_DOC.md §17），开发时以已确认决策为准，不得擅自变更。

---

## 2. 工作前提（必读）

1. **先读文档再动手**：任何涉及核心流程、调度算法、数据模型、离线包的改动，必须先阅读 TECH_DOC.md 对应章节；涉及产品行为的改动须对照 PRD.md。
2. **先对齐再实现**：发现文档与实现冲突，或需要偏离 PRD 已确认决策时，先停下并向用户说明，不得擅自变更。
3. **先更新文档再改结构**：修改架构、数据结构、算法或目录结构时，必须同步更新 TECH_DOC.md（以及本文件，如涉及目录结构），再写代码。
4. **小步提交**：每次改动保持范围最小、可独立评审、可独立回滚。

---

## 3. 项目结构规范

### 3.1 目标目录结构

```text
lib/
  main.dart
  app/                    # 应用装配：主题、路由、国际化、依赖注入
  core/                   # 基础设施：Result、日志、时间工具、常量
  data/
    local/                # Drift 表定义（tables/）、DAO、迁移脚本
    repositories/         # 仓储实现（domain/services 中接口的具体实现）
    sources/              # 词库导入、离线包下载、音频在线源
  domain/
    models/               # 领域模型
    scheduling/           # FSRS 引擎（fsrs/）、Scheduler 接口、队列构建与排序、软上限
    sessions/             # 学习/复习会话状态机
    services/             # 仓储接口（契约）+ 每日计划、打卡/统计、倒计时计划、导出
  features/
    onboarding/
    home/
    learn/
    review/
    results/
    stats/
    settings/
    about/
tools/
  content_pipeline/       # 词库构建、例句筛选、TTS、打包（离线工具链）
test/
  domain/
  integration/
```

### 3.2 结构纪律

- 新代码**必须落入上述分层**，不得新建"万能目录"（如 `lib/utils`、`lib/common`）堆积杂项；确实通用的工具放 `core/` 并保持命名明确。
- **分层依赖只允许向下**：`features` → `domain`/`data`，`data` → `domain`，`domain` 不依赖 Flutter/Android API，不依赖 `data` 层。
- 一个文件一个职责；文件内超过约 400 行时优先拆分，而不是继续追加。
- 新增功能目录时按 `features/<feature>/` 组织，内含页面、组件与状态。
- 禁止把业务逻辑写进 Widget、把 SQL 写进 UI；查询必须收敛到 `data/repositories/` 或 DAO。
- 数据库表结构以 TECH_DOC.md §8 为准；新增/修改字段必须附带迁移脚本（见第 7 节）。

---

## 4. 注释规范（关键注释）

### 4.1 原则

注释用于解释**"为什么"**，不解释"是什么"。代码本身应当清晰到大部分"是什么"无需注释。

### 4.2 必须写注释的场景

- **算法与公式**：FSRS 状态转移、间隔计算、逾期严重度排序、desired retention 等，注明公式来源与参数含义。
- **业务规则**：复习软上限、会话内重排上限（2 次）、评分映射（认识/模糊/不认识 → Again/Hard/Good）、乱序种子逻辑等"看似随意实则有意"的常量与判断。
- **非显而易见的时序/并发**：数据库单写连接、WAL、批量事务、后台下载保活、通知权限处理等。
- **数据迁移**：每条迁移脚本注明变更原因与影响范围。
- **Workaround**：第三方库或系统限制的绕过写法，必须注明原因与后续移除条件。
- **TODO/FIXME**：必须带上下文说明和负责人/日期，禁止裸 TODO。

### 4.3 禁止的注释

- 逐行翻译式注释（`// 增加计数` 这类与代码等价的注释）。
- 冗余的"代码块上方复述代码"注释。
- 过时注释：修改代码时必须同步修改或删除相关注释，不得保留描述旧行为的注释。

### 4.4 语言与格式

- 注释统一使用**简体中文**；代码标识符、关键词、公式保留英文。
- 单行注释用 `//`；模块/类/复杂函数用 `///` 文档注释，简述职责、参数与返回。
- 文件头（可选但推荐）：每个核心文件顶部用一行注释说明职责，例如：

```dart
/// 复习队列构建器：按逾期严重度排序并应用复习软上限。
class ReviewQueueBuilder { ... }
```

---

## 5. Git 版本管理

### 5.1 分支策略

- 主分支 `main` 长期存在且**受保护**，始终可发布。
- 远程仓库为 GitHub（`origin`），推送前确认远程地址正确、凭据有效。
- 功能分支命名：`feature/<简述>`、`fix/<简述>`、`docs/<简述>`、`chore/<简述>`。
- 分支生命周期短（目标 ≤ 3 天），合并后删除。
- 禁止直接向 `main` 推送未经评审的改动；单人开发时也至少保持 `main` 与开发分支分离。

### 5.2 Commit 规范（Conventional Commits）

```text
<type>(<scope>): <描述>
```

- type：`feat` / `fix` / `docs` / `refactor` / `perf` / `test` / `chore` / `build` / `ci`
- scope：模块名，如 `scheduling`、`session`、`db`、`offline-pack`、`settings`，可省略。
- 描述用祈使句；中英文均可，但同一仓库内保持一致（建议中文，如 `feat(scheduling): 引入 FSRS-5 状态转移`）。
- **一个 commit 只做一件事**；提交粒度要小，便于回滚与评审。

禁止：

- 混合无关改动（如重构与修 bug 同提交）。
- 提交生成物、密钥、大文件（见 5.3）。
- 空泛描述（`update`、`fix stuff`、`wip`）。

### 5.3 提交内容边界

- 必须维护并提交 `.gitignore`，至少包含：`build/`、`.dart_tool/`、`*.iml`、`.idea/`、`.vscode/`（按需）、本地密钥/配置文件、数据库文件、离线包与词库发布产物。
- **不进入源码仓库**：构建产物、词库 DB、音频离线包（体积大、变动频繁，走独立发布流程）、任何密钥或含敏感信息的文件。
- 单个文件超过约 5 MB（尤其二进制）不得提交，除非是经确认的必要资产。
- 词库/音频等发布产物使用 `tag` + release（或对象存储）管理，版本号与 App 代码版本解耦。

### 5.4 历史整洁

- 已推送的分支**不得随意 rebase**；仅可对自己刚推送、未共享的分支做整理。
- 合并分支默认使用 `--no-ff` 保留合并意图；小改动可 squash 合并。
- 版本发布打 tag：`v<major>.<minor>.<patch>`（如 `v0.1.0`），tag 必须指向 main 上可构建的提交。
- 内容管线产物版本独立编号（如 `wordbook-gaokao-3500-v1.2`），与代码 tag 不混用。

### 5.5 危险操作红线

- 禁止使用 `git reset --hard`、`git checkout --`、`git clean -fd` 等破坏性命令，除非用户明确要求且已确认影响范围。
- 删除或重命名文件使用 `git mv`/常规 `git rm` 并随提交记录，删除前先检查引用。
- 需要回滚时优先 `git revert`，而不是重写历史。

---

## 6. 开发流程约束

1. **算法改动必须有测试**：FSRS 相关改动必须跑通官方 golden 用例（精度 1e-6），并补充边界用例。
2. **会话与进度改动必须验证续学**：涉及学习/复习会话、快照恢复的改动，必须通过"中断 → 恢复"类测试。
3. **数据库变更必须带迁移**：schema 变更随代码提交迁移脚本与降级说明；词库升级不得破坏用户进度（word_id 映射策略见 TECH_DOC.md §8.2）。
4. **提交前自检**：`flutter analyze` 无新增警告、`flutter test` 全绿；违反时不得宣称完成。
5. **不引入无关依赖**：新增第三方包必须说明用途与体积影响，优先选纯 Dart/离线可用方案。

---

## 7. 测试要求

- `domain/` 核心逻辑（FSRS、队列排序、会话状态机、每日计划、统计）必须有单元测试。
- 主流程必须有集成测试：Onboarding → 学习 → 复习 → 完成页。
- 离线场景（飞行模式）与中断续学必须有自动化或明确的手动验证步骤。
- 测试代码同样遵守结构纪律，不允许为了通过测试而绕过分层（如直接 mock 数据库细节）。

---

## 8. 禁止事项（红线）

- 引入广告、统计、支付、社交类 SDK 或任何与记忆无关的功能。
- 收集、上传或向第三方共享用户学习数据（含崩溃日志中的敏感字段）。
- 在代码中硬编码密钥、Token 或个人信息。
- 使用 `rm -rf` 等破坏性命令清理目录、或在未确认的情况下删除用户数据。
- 修改 PRD 已确认决策（免费、无账号、本地优先、复习软上限、评分映射等）而不与用户确认并更新文档。
- 在未阅读 PRD.md / TECH_DOC.md 对应章节的情况下实现核心功能。

---

## 9. 文档同步规则

| 变更类型 | 必须同步的文档 |
|---|---|
| 架构、分层、目录结构 | TECH_DOC.md + 本文件 §3 |
| 数据结构、迁移 | TECH_DOC.md §8 |
| 算法、调度、队列 | TECH_DOC.md §6/§7 |
| 产品行为、功能范围 | PRD.md（与用户确认后） |
| 依赖、技术选型 | TECH_DOC.md §3 |

完成一项功能后，若文档与实现存在出入，以"文档更新完成"作为该任务的收尾条件。

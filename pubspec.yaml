name: happy_bei_dan_ci
description: "我爱背单词：本地优先、离线可用的高中英语单词记忆应用（M1 工程骨架）。"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  archive: ^4.0.9
  crypto: ^3.0.7
  drift: ^2.34.3
  drift_flutter: ^0.3.1
  flutter:
    sdk: flutter
  flutter_local_notifications: ^22.3.0
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  go_router: ^17.5.0
  intl: ^0.20.2
  just_audio: ^0.10.6
  path_provider: ^2.1.6
  share_plus: ^13.3.0
  sqlite3: ^3.5.1
  timezone: ^0.11.1
  workmanager: ^0.10.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.15.1
  drift_dev: ^2.34.5

flutter:
  uses-material-design: true
  generate: true
